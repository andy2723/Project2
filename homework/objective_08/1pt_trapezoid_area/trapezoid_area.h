/**
 * Beginner: Update the function so that it stores strings of user input to a
 * vector until the user enters "Quit"
 */
#pragma once

double *trapezoidArea(double base_a, double base_b, double height);
